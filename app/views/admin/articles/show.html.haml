.pull-right
  = link_to "Back to list", admin_articles_path, :class => 'btn'
  = link_to "Edit", edit_admin_article_path(@article), :class => 'btn'
  = link_to "Delete", admin_article_path(@article), :method => :delete, :confirm => "Are you sure?", :class => 'btn btn-danger'

= render :partial => 'articles/article', :object => @article
		
